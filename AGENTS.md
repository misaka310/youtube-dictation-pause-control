# Repository instructions

## 仕様の正本

- 仕様の正本は `README.md` と対応する `docs/`。
- 仕様変更時は、実装・テスト・利用者向け文書を同じ変更で揃える。

## Repository boundaries

- このリポジトリは、Typeless / Wispr Flowの入力状態からYouTubeの一時停止・再開を制御する独立したWindowsツールとして完結させる。
- 別リポジトリ、個人ワークスペース、共有Skill、専用監視サービス、開発者個人のブラウザ状態を前提にしない。
- 外部ツール固有の統合コードや運用手順を持ち込まず、必要な拡張点は公開されたローカルHTTP APIとして一般化する。

## ブラウザ拡張機能

- 拡張機能の読み込み対象は `extension/`。
- 通常利用に不要な開発者専用ホスト権限、ローカル制御ポート、自動リロード常駐処理をmanifestやbackground workerへ追加しない。
- ブラウザE2Eは隔離環境で実行し、利用者の既存タブや既存プロファイルへ依存しない。

## Verification

- リポジトリに記載されたtest、build、GUI smokeの入口を使用する。
- 仕様、実装、テスト、利用者向け文書が一致するまで完了扱いにしない。
